# Polymarket 尾盘交易策略

自动监控 Polymarket Sport 市场价格，当价格达到入场条件时自动买入，并在价格跌破止损线时自动卖出的交易机器人。

## 功能特性

- 🔍 **自动扫描** - 自动扫描 Sport 市场，过滤即将结算的尾盘市场
- 📊 **价格监控** - 实时监控持仓市场价格
- 🎯 **自动入场** - 当价格达到设定值时自动买入
- 🛡️ **止损保护** - 价格跌破止损线自动卖出
- 📱 **Telegram 通知** - 买入/卖出时发送 Telegram 消息
- 🖥️ **Web 界面** - 简洁的管理界面，配置策略参数
- 📝 **日志记录** - 中文日志，每2小时自动切割

## 策略说明

### 尾盘策略逻辑

1. **市场扫描**: 定期扫描所有 Sport 类市场
2. **时间过滤**: 只关注距离结算时间在配置范围内的市场（默认1小时）
3. **价格过滤**: 当 YES 价格 ≥ 入场价（默认90）时触发入场信号
4. **自动买入**: 如果启用自动交易，会自动下单买入
5. **止损监控**: 持续监控持仓市场价格
6. **止损执行**: 当价格 ≤ 止损价（默认85）时自动卖出

### 风险控制

- 单次下单金额限制
- 最大持仓金额限制
- 最大持仓数量限制
- 每日最大亏损限制

## 快速开始

### 环境要求

- Python 3.11+
- Docker (可选)

### 本地运行

1. **克隆项目**
```bash
git clone <repo-url>
cd polymarket-trader
```

2. **安装依赖**
```bash
pip install -r requirements.txt
```

3. **配置环境变量**
```bash
cp .env.example .env
# 编辑 .env 文件，填入你的配置
```

4. **启动服务**
```bash
python -m uvicorn app.main:app --host 0.0.0.0 --port 9000
```

5. **访问界面**
```
http://localhost:9000
```

### Docker 运行

1. **构建镜像**
```bash
docker build -t polymarket-trader .
```

2. **运行容器**
```bash
docker run -d \
  --name polymarket-trader \
  -p 9000:9000 \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/logs:/app/logs \
  -e POLY_PRIVATE_KEY=your_private_key \
  -e TG_BOT_TOKEN=your_bot_token \
  -e TG_CHAT_ID=your_chat_id \
  polymarket-trader
```

3. **查看日志**
```bash
docker logs -f polymarket-trader
```

4. **停止容器**
```bash
docker stop polymarket-trader
```

## 配置说明

### 环境变量

| 变量名 | 说明 | 必填 |
|--------|------|------|
| `POLY_PRIVATE_KEY` | 钱包私钥 | ✅ |
| `POLY_API_KEY` | API Key (可选，会自动派生) | ❌ |
| `POLY_API_SECRET` | API Secret | ❌ |
| `POLY_API_PASSPHRASE` | API Passphrase | ❌ |
| `TG_BOT_TOKEN` | Telegram Bot Token | ❌ |
| `TG_CHAT_ID` | Telegram Chat ID | ❌ |
| `HOST` | 服务监听地址 | ❌ |
| `PORT` | 服务端口 | ❌ |

### 策略参数 (可通过 Web 界面配置)

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 入场价格 | 90 | 价格 ≥ 此值时买入 (0-100) |
| 止损价格 | 85 | 价格 ≤ 此值时卖出 (0-100) |
| 单次金额 | $10 | 每次买入金额 (USDC) |
| 最大持仓 | $100 | 最大持仓金额 |
| 时间过滤 | 1小时 | 结算前多少小时内的市场 |
| 扫描间隔 | 30秒 | 市场扫描间隔 |
| 最大亏损 | $50 | 每日最大亏损限制 |
| 最大持仓数 | 5 | 最多同时持有几个仓位 |

## API 接口

### 系统状态
- `GET /api/status` - 获取系统状态
- `POST /api/start` - 启动交易
- `POST /api/stop` - 停止交易
- `GET /health` - 健康检查

### 配置管理
- `GET /api/config/trading` - 获取交易配置
- `PUT /api/config/trading` - 更新交易配置
- `GET /api/config/telegram` - 获取 Telegram 配置
- `PUT /api/config/telegram` - 更新 Telegram 配置
- `POST /api/config/telegram/test` - 测试 Telegram 连接

### 账户信息
- `GET /api/account/balance` - 获取账户余额
- `GET /api/account/positions` - 获取当前持仓

### 市场信息
- `GET /api/markets/sport` - 获取 Sport 市场列表
- `GET /api/markets/monitored` - 获取监控中的市场
- `GET /api/markets/{token_id}/price` - 获取市场价格

### 交易操作
- `POST /api/trade/buy` - 手动买入
- `POST /api/trade/sell/{market_id}` - 手动卖出

### 历史记录
- `GET /api/orders/recent` - 获取最近订单
- `GET /api/stats/daily` - 获取每日统计
- `GET /api/positions/history` - 获取仓位历史

## 项目结构

```
polymarket-trader/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI 应用入口
│   ├── config.py            # 配置管理
│   ├── models.py            # 数据模型
│   ├── database.py          # SQLite 数据库
│   ├── api/
│   │   ├── __init__.py
│   │   └── routes.py        # API 路由
│   ├── services/
│   │   ├── __init__.py
│   │   ├── polymarket.py    # Polymarket API 封装
│   │   ├── trader.py        # 交易逻辑
│   │   └── telegram.py      # Telegram 通知
│   └── utils/
│       ├── __init__.py
│       └── logger.py        # 日志工具
├── frontend/
│   └── index.html           # Web 界面
├── data/                    # 数据目录
├── logs/                    # 日志目录
├── Dockerfile
├── .dockerignore
├── .env.example
├── requirements.txt
└── README.md
```

## 注意事项

⚠️ **风险警告**

1. 本项目仅供学习和研究使用
2. 加密货币交易存在高风险，请谨慎投资
3. 请勿投入超出承受范围的资金
4. 建议先使用小额资金测试策略
5. 妥善保管私钥，不要泄露给任何人

## License

MIT License
